import React from 'react'
import {newLogo} from '../assets'

const Hero = () => {
  console.log(newLogo)
  return (
    <header className='w-full flex justify-center items-center flex-col'>
      <nav className="flex justify-between items-cener flex-col">
        {/* <img src={} alt="" /> */}
      </nav>
    </header>
  )
}

export default Hero
